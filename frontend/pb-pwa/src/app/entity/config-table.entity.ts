/**
 * ConfigTable -
 * Entity-Class used to hold common table
 * configuration-data
 */
export class ConfigTable {
  /**
   * Constructor
   */
  constructor(
    /**
     * A set of common configuration-data
     */
    public config: any,
  ) { }
}
